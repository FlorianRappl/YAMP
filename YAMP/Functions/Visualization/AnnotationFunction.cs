using System;
using YAMP.Converter;

namespace YAMP
{
    /*
    class AnnotationFunction : SystemFunction
    {
        public void Function(PlotValue plot, StringValue op, ArgumentsValue args)
        {
            var ops = new StringToEnumConverter(typeof(AnnotationOperations));
            var _op = (AnnotationOperations)ops.Convert(op);

            switch (_op)
            {
                case AnnotationOperations.Add:

                    break;
                case AnnotationOperations.Edit:

                    break;
                case AnnotationOperations.Del:
                    
                    break;
            }
        }

        #region Helpers

        enum AnnotationOperations
        {
            Add,
            Del,
            Edit
        }

        #endregion
    }*/
}
